/// Whether the active platform's video encoder ignores app-specified bitrate caps and
/// instead uses its own preset defaults (iOS `AVAssetExportSession`).
bool encoderIgnoresPerBitrateCap() => false;
