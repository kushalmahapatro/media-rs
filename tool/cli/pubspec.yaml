name: media_cli
description: >
  CLI to populate repo platform-builds/ (Rust + FFmpeg layout) for prebuilt hooks.
version: 0.1.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.6.0
  logging: ^1.3.0
  path: ^1.9.1
  media_native_build:
    path: ../native_build

executables:
  media_cli: media_cli
